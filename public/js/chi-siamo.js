// JavaScript semplificato per la pagina Chi Siamodocument.addEventListener('DOMContentLoaded', function() {    console.log('Chi siamo page loaded');        // Animazioni di fade-in semplici per le sezioni    const animateElements = document.querySelectorAll('.story-card, .value-card, .timeline-item, .team-member');        // Observer per animazioni scroll-based semplici    const observer = new IntersectionObserver((entries) => {        entries.forEach(entry => {            if (entry.isIntersecting) {                entry.target.style.opacity = '1';                entry.target.style.transform = 'translateY(0)';            }        });    }, {        threshold: 0.1,        rootMargin: '0px 0px -50px 0px'    });    // Applica stili iniziali e osserva elementi    animateElements.forEach(el => {        el.style.opacity = '0';        el.style.transform = 'translateY(20px)';        el.style.transition = 'opacity 0.6s ease, transform 0.6s ease';        observer.observe(el);    });    // Smooth scroll per link interni    document.querySelectorAll('a[href^="#"]').forEach(anchor => {        anchor.addEventListener('click', function (e) {            e.preventDefault();            const target = document.querySelector(this.getAttribute('href'));            if (target) {                target.scrollIntoView({                    behavior: 'smooth',                    block: 'start'                });            }        });    });    // Timeline responsiva - gestione semplice    function adjustTimeline() {        const timeline = document.querySelector('.timeline');        if (timeline && window.innerWidth < 768) {            timeline.classList.add('mobile-timeline');        } else if (timeline) {            timeline.classList.remove('mobile-timeline');        }    }    // Chiama all'avvio e su resize    adjustTimeline();    window.addEventListener('resize', adjustTimeline);    // Gestione errori immagini team    const teamImages = document.querySelectorAll('.team-member img');    teamImages.forEach(img => {        img.addEventListener('error', function() {            this.src = '/images/default-avatar.png';        });    });});