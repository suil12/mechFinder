<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Contatti - MechFinder, trova il meccanico più adatto a te">
    <meta name="keywords" content="meccanici, contatti, assistenza, riparazioni auto, officina">
    <meta name="author" content="MechFinder">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <title>Servizi - MechFinder</title>
</head>
<body>
    <%- include('./common/header') %>

    <main>
        <!-- Hero section -->
        <section class="bg-primary text-white py-5 mb-5">
            <div class="container">
                <div class="row align-items-center">
                    <div class="col-lg-6">
                        <h1 class="display-4 fw-bold mb-3">I Nostri Servizi</h1>
                        <p class="lead mb-4">Offriamo una gamma completa di servizi per la manutenzione e riparazione del tuo veicolo. I nostri meccanici qualificati sono pronti ad aiutarti con qualsiasi problema.</p>
                        <a href="/meccanici" class="btn btn-light btn-lg">Trova un meccanico</a>
                    </div>
                    <div class="col-lg-6 d-none d-lg-block">
                        <img src="/media/img/services-hero.jpg" alt="Servizi di riparazione auto" class="img-fluid rounded">
                    </div>
                </div>
            </div>
        </section>

        <!-- Servizi overview -->
        <section class="container mb-5">
            <div class="text-center mb-5">
                <h2 class="fw-bold">Soluzioni Per Ogni Esigenza</h2>
                <p class="lead text-muted">Scopri tutti i servizi offerti dai nostri meccanici professionisti.</p>
            </div>
            
            <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
                <% servizi.forEach(servizio => { %>
                    <div class="col">
                        <div class="service-card">
                            <div class="service-icon">
                                <% if (servizio.nome === 'Elettronica') { %>
                                    <i class="fas fa-microchip"></i>
                                <% } else if (servizio.nome === 'Meccanica Generale') { %>
                                    <i class="fas fa-cogs"></i>
                                <% } else if (servizio.nome === 'Revisioni') { %>
                                    <i class="fas fa-clipboard-check"></i>
                                <% } else if (servizio.nome === 'Tagliandi') { %>
                                    <i class="fas fa-oil-can"></i>
                                <% } else if (servizio.nome === 'Cambio Gomme') { %>
                                    <i class="fas fa-tire"></i>
                                <% } else if (servizio.nome === 'Carrozzeria') { %>
                                    <i class="fas fa-car-side"></i>
                                <% } else { %>
                                    <i class="fas fa-tools"></i>
                                <% } %>
                            </div>
                            <h3 class="h4 mb-3"><%= servizio.nome %></h3>
                            <p class="mb-3"><%= servizio.descrizione %></p>
                            <a href="#<%= servizio.nome.toLowerCase().replace(/\s+/g, '-') %>" class="btn btn-outline-primary">Scopri di più</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </section>

        <!-- Dettaglio servizi -->
        <% servizi.forEach((servizio, index) => { %>
            <section id="<%= servizio.nome.toLowerCase().replace(/\s+/g, '-') %>" class="service-section">
                <div class="container">
                    <div class="row align-items-center <%= index % 2 === 0 ? '' : 'flex-row-reverse' %>">
                        <div class="col-lg-6 mb-4 mb-lg-0">
                            <img src="/media/img/<%= servizio.icona %>" alt="<%= servizio.nome %>" class="service-img">
                        </div>
                        <div class="col-lg-6">
                            <h2 class="fw-bold mb-4"><%= servizio.nome %></h2>
                            <p class="lead mb-4"><%= servizio.descrizione %></p>
                            
                            <!-- Dettagli specifici per ogni servizio -->
                            <% if (servizio.nome === 'Elettronica') { %>
                                <ul class="list-unstyled mb-4">
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Diagnosi elettronica completa</li>
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Riparazione di centraline</li>
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Installazione impianti audio e navigazione</li>
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Risoluzione problemi elettrici</li>
                                </ul>
                            <% } else if (servizio.nome === 'Meccanica Generale') { %>
                                <ul class="list-unstyled mb-4">
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Riparazione e sostituzione motori</li>
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Manutenzione del cambio e della trasmissione</li>
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Riparazione freni e sospensioni</li>
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Diagnostica problemi meccanici</li>
                                </ul>
                            <% } else if (servizio.nome === 'Revisioni') { %>
                                <ul class="list-unstyled mb-4">
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Revisioni ministeriali ufficiali</li>
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Controllo emissioni</li>
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Verifica completa di sicurezza</li>
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Pre-revisione con check-up gratuito</li>
                                </ul>
                            <% } else if (servizio.nome === 'Tagliandi') { %>
                                <ul class="list-unstyled mb-4">
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Cambio olio e filtri</li>
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Controllo liquidi e livelli</li>
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Tagliandi programmati per ogni marca</li>
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Manutenzione ordinaria completa</li>
                                </ul>
                            <% } else if (servizio.nome === 'Cambio Gomme') { %>
                                <ul class="list-unstyled mb-4">
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Sostituzione pneumatici estivi e invernali</li>
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Bilanciatura e convergenza</li>
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Riparazione forature</li>
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Deposito gomme stagionali</li>
                                </ul>
                            <% } else if (servizio.nome === 'Carrozzeria') { %>
                                <ul class="list-unstyled mb-4">
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Riparazione di ammaccature e graffi</li>
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Verniciatura professionale</li>
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Sostituzione parabrezza e vetri</li>
                                    <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Ripristino danni da incidente</li>
                                </ul>
                            <% } %>
                            
                            <a href="/meccanici" class="btn btn-primary">Trova un meccanico</a>
                        </div>
                    </div>
                </div>
            </section>
        <% }) %>

        <!-- CTA -->
        <section class="bg-primary text-white py-5 my-5">
            <div class="container">
                <div class="row align-items-center">
                    <div class="col-lg-8 mb-4 mb-lg-0">
                        <h2 class="fw-bold mb-3">Hai bisogno di assistenza?</h2>
                        <p class="lead mb-0">Contatta uno dei nostri meccanici professionisti e ricevi un preventivo gratuito per la tua auto.</p>
                    </div>
                    <div class="col-lg-4 text-lg-end">
                        <a href="/meccanici" class="btn btn-light btn-lg">Trova un meccanico</a>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <%- include('./common/footer') %>
    <%- include('./common/modals') %>
    <%- include('./common/scripts') %>
</body>
</html>