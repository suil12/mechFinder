<!DOCTYPE html>
<html lang="it">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="description" content="Contatti - MechFinder, trova il meccanico più adatto a te">
        <meta name="keywords" content="meccanici, contatti, assistenza, riparazioni auto, officina">
        <meta name="author" content="MechFinder">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
        <link rel="stylesheet" href="/css/style.css">
        <title>Contatti - MechFinder</title>
      </head>
<body>
    <%- include('./common/header') %>
<main>
  <div class="container mb-5">
    <div class="row">
      <div class="col-12">
        <div class="dashboard-welcome my-4">
          <h1>Benvenuto nella tua Dashboard, <%= user.nome %></h1>
          <p class="lead">Da qui puoi gestire le tue riparazioni, i tuoi veicoli e visualizzare le tue statistiche.</p>
        </div>
      </div>
    </div>
    <div class="row mt-4">
      <!-- Statistiche rapide -->
      <div class="col-md-4 mb-4">
        <div class="card">
          <div class="card-header">
            <h5 class="card-title mb-0">Statistiche</h5>
          </div>
          <div class="card-body">
            <div class="d-flex justify-content-between mb-3">
              <div>Riparazioni in corso:</div>
              <div class="badge bg-primary"><%= riparazioniInCorso || 0 %></div>
            </div>
            <div class="d-flex justify-content-between mb-3">
              <div>Riparazioni completate:</div>
              <div class="badge bg-success"><%= riparazioniCompletate || 0 %></div>
            </div>
            <div class="d-flex justify-content-between">
              <div>Valutazione media:</div>
              <div class="text-warning">
                <% if (valutazioneMedia) { %>
                  <i class="fas fa-star"></i> <%= valutazioneMedia.toFixed(1) %>
                <% } else { %>
                  N/A
                <% } %>
              </div>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Riparazioni recenti -->
      <div class="col-md-8 mb-4">
        <div class="card">
          <div class="card-header d-flex justify-content-between align-items-center">
            <h5 class="card-title mb-0">Riparazioni Recenti</h5>
            <a href="/meccanico/riparazioni" class="btn btn-sm btn-primary">Vedi tutte</a>
          </div>
          <div class="card-body">
            <% if (riparazioni && riparazioni.length > 0) { %>
              <div class="table-responsive">
                <table class="table table-hover">
                  <thead>
                    <tr>
                      <th>Cliente</th>
                      <th>Descrizione</th>
                      <th>Stato</th>
                      <th>Data</th>
                      <th>Azioni</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% riparazioni.slice(0, 5).forEach(riparazione => { %>
                      <tr>
                        <td><%= riparazione.nome_cliente || 'N/A' %></td>
                        <td><%= riparazione.descrizione.substring(0, 30) %>...</td>
                        <td>
                          <span class="badge <%= getStatusBadgeClass(riparazione.stato) %>">
                            <%= translateStatus(riparazione.stato) %>
                          </span>
                        </td>
                        <td><%= formatDate(riparazione.data_richiesta) %></td>
                        <td>
                          <a href="/riparazioni/<%= riparazione.id %>" class="btn btn-sm btn-primary">
                            <i class="fas fa-eye"></i>
                          </a>
                        </td>
                      </tr>
                    <% }); %>
                  </tbody>
                </table>
              </div>
            <% } else { %>
              <div class="alert alert-info">Non hai ancora nessuna riparazione.</div>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </div>
</main>

<%- include('./common/footer') %>
<%- include('./common/modals') %>
<%- include('./common/scripts') %>
</body>
</html>