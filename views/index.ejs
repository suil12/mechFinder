<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="MechFinder - Trova il meccanico giusto per te">
    <meta name="keywords" content="meccanici, riparazioni auto, officina, auto, moto">
    <meta name="author" content="MechFinder">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <title>MechFinder - Trova il meccanico giusto per te</title>
</head>
<body>
    <%- include('./common/header') %>
    <main>
        <!-- Banner e ricerca -->
        <section class="section1">
            <div class="banner">
                <div class="container">
                    <div class="row justify-content-center">
                        <div class="col-lg-10 col-xl-8">
                            <h1>Trova il meccanico giusto per la tua auto</h1>
                            <h5>Connettiti con professionisti qualificati della tua zona</h5>
                            <div class="d-flex justify-content-center w-100">
                                <div class="search-cont" style="max-width: 500px; width: 100%;">
                                    <form action="/search" method="GET">
                                        <input type="text" name="q" class="search-input" placeholder="Cerca meccanici, servizi o città..." value="<%= query %>">
                                        <button type="submit" class="search-button">
                                            <i class="fas fa-search"></i>
                                        </button>
                                    </form>
                                </div>
                            </div>
                            
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Servizi offerti -->
        <section class="py-5">
            <div class="container">
                <div class="text-center mb-5">
                    <h2 class="fw-bold">I nostri servizi</h2>
                    <p class="lead text-muted">Risolviamo qualsiasi problema della tua auto</p>
                </div>
                
                <div class="row g-4 justify-content-center">
                    <% servizi.forEach(servizio => { %>
                        <div class="col-lg-4 col-md-6 d-flex justify-content-center">
                            <div class="servizio-card">
                                <div class="servizio-card-front">
                                    <img src="/media/img/<%= servizio.icona %>" alt="<%= servizio.nome %>">
                                    <h4><%= servizio.nome %></h4>
                                </div>
                                <div class="servizio-card-back">
                                    <p><%= servizio.descrizione %></p>
                                </div>
                            </div>
                        </div>                        
                    <% }); %>
                </div>
                
                <div class="text-center mt-4">
                    <a href="/servizi" class="btn btn-primary">Scopri tutti i servizi</a>
                </div>
            </div>
        </section>

        <!-- Meccanici in evidenza -->
        <section class="card_mecc py-5 bg-light">
            <div class="container">
                <h1 class="text-center mb-4">I Meccanici Più Valutati</h1>
                
                <div class="slider">
                    <% meccaniciEvidenziati.forEach(meccanico => { %>
                        <div class="mechanic-card" data-id="<%= meccanico.id %>">
                            <img src="<%= meccanico.avatar || '/media/img/default_mechanic.png' %>" alt="<%= meccanico.nome %> <%= meccanico.cognome %>">
                            <h3><%= meccanico.nome_officina || `${meccanico.nome} ${meccanico.cognome}` %></h3>
                            <p><%= meccanico.specializzazione %></p>
                            <div class="rating">
                                <% for(let i=0; i<Math.floor(meccanico.valutazione); i++) { %>★<% } %>
                                <% for(let i=0; i<(5-Math.floor(meccanico.valutazione)); i++) { %>☆<% } %>
                                <span class="rating-text"><%= meccanico.valutazione.toFixed(1) %></span>
                            </div>
                            <a href="/meccanici/<%= meccanico.id %>" class="btn-contact">Visualizza profilo</a>
                        </div>
                    <% }); %>
                </div>
                
                <div class="text-center mt-4">
                    <a href="/meccanici" class="btn btn-outline-primary">Vedi tutti i meccanici</a>
                </div>
            </div>
        </section>

        <!-- Recensioni dei clienti -->
        <section class="reviews py-5">
            <div class="container">
                <h2 class="text-center mb-4">Cosa dicono i nostri clienti</h2>
                
                <div class="row justify-content-center">
                    <% recensioni.forEach(recensione => { %>
                        <div class="col-lg-4 col-md-6 mb-4">
                            <div class="review-card">
                                <div class="review-rating">
                                    <% for(let i=0; i<recensione.valutazione; i++) { %>★<% } %>
                                    <% for(let i=0; i<(5-recensione.valutazione); i++) { %>☆<% } %>
                                </div>
                                <p class="review-text">"<%= recensione.commento %>"</p>
                                <small class="review-author">- <%= recensione.nome_cliente %> <%= recensione.cognome_cliente %></small>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </div>
        </section>

        <!-- Sezione CTA per Meccanici -->
        <section class="py-5 bg-light">
            <div class="container">
                <div class="row align-items-center justify-content-center">
                    <div class="col-lg-7 mb-4 mb-lg-0">
                        <h2 class="fw-bold mb-3">Sei un meccanico?</h2>
                        <p class="lead">Entra a far parte della nostra rete di professionisti e amplia il tuo business.</p>
                        <ul class="list-unstyled">
                            <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Ricevi richieste di preventivo nella tua zona</li>
                            <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Gestisci il tuo profilo professionale</li>
                            <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Ottieni recensioni e costruisci la tua reputazione</li>
                            <li class="mb-2"><i class="fas fa-check text-primary me-2"></i> Nessun costo fisso, paghi solo per le richieste accettate</li>
                        </ul>
                        <button type="button" class="btn btn-primary mt-3" data-bs-toggle="modal" data-bs-target="#registerMeccanicoModal">
                            Registrati come meccanico <i class="fas fa-arrow-right ms-2"></i>
                        </button>
                    </div>
                    <div class="col-lg-5">
                        <img src="/media/img/meccanico.jpg" alt="Meccanico al lavoro" class="img-fluid rounded shadow">
                    </div>
                </div>
            </div>
        </section>
    </main>

    <%- include('./common/footer') %>
    <%- include('./common/modals') %>
    <%- include('./common/scripts') %>
</body>
</html>