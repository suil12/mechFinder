<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="Recupera password - MechFinder">
  <meta name="keywords" content="reset, password, meccanici, recupero">
  <meta name="author" content="MechFinder">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/footer.css">
  <title>Recupera password - MechFinder</title>
</head>
<body>
  <%- include('../common/navbar') %>
  
  <main>
    <div class="container col-md-6 col-lg-5 mt-5">
      <div class="card shadow">
        <div class="card-header text-center py-3">
          <h4 class="mb-0">Recupera la tua password</h4>
        </div>
        <div class="card-body p-4">
          <% if (error && error.length > 0) { %>
            <div class="alert alert-danger" role="alert">
              <%= error %>
            </div>
          <% } %>
          <% if (success && success.length > 0) { %>
            <div class="alert alert-success" role="alert">
              <%= success %>
            </div>
          <% } %>
          
          <p class="text-muted mb-4">Inserisci l'indirizzo email associato al tuo account. Ti invieremo un link per reimpostare la password.</p>
          
          <form action="/auth/reset-password" method="POST">
            <div class="mb-3">
              <label for="email" class="form-label">Email</label>
              <input type="email" class="form-control" id="email" name="email" required>
            </div>
            
            <div class="d-grid mt-4">
              <button type="submit" class="btn btn-primary">Invia link di reset</button>
            </div>
          </form>
          
          <div class="text-center mt-4">
            <p class="mb-0">Ricordi la password? <a href="/" class="text-decoration-none">Torna alla home</a></p>
          </div>
        </div>
      </div>
    </div>
  </main>
  
  <%- include('../common/footer') %>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>